#include "lib/screen.whl"

proc main()
    clearScreen()

    number i

    for i = 0 to 10 do
    	number x
    	x = i
    	x *= 20
    	number y
    	y = i
    	y *= 12
    	drawNumber(x, y, i)
    end

label:
    jmp label
endp