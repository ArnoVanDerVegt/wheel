#project "Struct demo"
    
#include "/lib/standard.whl"
    
struct Point
    number   x, y, z
ends
    
Point    point
    
proc main()
    set      point.x,                7
    set      point.y,                13
    set      point.z,                19
    
    printN(point.x)
    printN(point.y)
    printN(point.z)
endp