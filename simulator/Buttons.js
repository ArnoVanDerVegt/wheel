(function() {
    var wheel = require('../utils/base.js').wheel;

    wheel(
        'simulator.Buttons',
        wheel.Class(function() {
            this.init = function(opts) {
            };
        })
    );
})();
